<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.learnprograming">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logoicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.LearnPrograming">
        <activity android:name=".LevelActivity"></activity>
        <activity android:name=".ExpandableLangDetailsActivity" />
        <activity android:name=".LanguageDetails" />
        <activity android:name=".ProgrammingLangActivity" />
         <activity android:name=".ContentActivity" />
        <activity android:name=".RegisterActivity" />
         <activity android:name=".LogInActivity">
         <intent-filter>
         <action android:name="android.intent.action.MAIN" />
         <category android:name="android.intent.category.LAUNCHER" />
         </intent-filter>
         </activity>

<!--        <activity android:name=".ContentActivity">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
<!--        </activity>-->
    </application>

</manifest>